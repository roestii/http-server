#include "linked_list_allocator.h"
#include <stddef.h>

i16 init(linked_list_allocator* alloc, arena_allocator* root, usize len)
{
	return 0;
}

void* allocate(linked_list_allocator* alloc, usize len)
{
	return NULL;
}

void free(linked_list_allocator* alloc, void* addr)
{
}
